FREE_SHIPPING = 'Free shipping only applies to single customer orders';
BANNED_WARNING = 'Unfortunately we do not ship to your country of residence';
NONE_SELECTED = '0';

const customers = '1';
let location = 'RSA' ;

if (location = 'RSA') {
     shipping === 400 && currency === 'R'
     } else if (location = 'NAM') {
         shipping = 600 && currency === '$'
        }else 
          shipping = 800 && currency ==='$'

shoes = 300 * 1
toys - 100 * 5
shirts = 150 * 'NONE_SELECTED'
batteries = 35 * 2
pens = 5 * 'NONE_SELECTED' 


if (location = 'NAM' || 'RSA' && customers < 2) {
    shoes + batteries + pens + shirts > 1000 || 60
    } calcShipping
		
		
if (shipping = 0 && customers === 1) 
 console.log(FREE_SHIPPING) 

location = 'NK' ? console.log(BANNED_WARNING) : console.log('price', currency, shoes + batteries + pens + shirts)


