const FREE_SHIPPING = 'Free shipping only applies to single customer orders'
const BANNED_WARNING = 'Unfortunately we do not ship to your country of residence'
const NONE_SELECTED = '0'

const customers = '1';
let country = 'RSA' ;
 const shipping = 400 ;
let currency = 'R'
const calcShipping = (shipping + currency)

if (country = 'RSA') {
     shipping === 400 && currency === 'R'
     } else if (country = 'NAM') {
         shipping = 600 && currency === '$'
        }else 
          shipping = 800 && currency ==='$'

 let shoes = 300
 let toys = 500
let shirts = 0
let batteries = 70
let pens = 0 


if (country = 'NAM' || 'RSA' && customers < 2) {
    shoes + batteries + pens + shirts >= 1000 || 60
    } calcShipping
		
		
if (shipping == 400 && customers == 1) 
 console.log(FREE_SHIPPING) 

if (country = 'NK') {
    console.log(BANNED_WARNING)
}
 console.log( 'Price:',currency, shoes + toys + shirts + batteries + pens + shipping)
